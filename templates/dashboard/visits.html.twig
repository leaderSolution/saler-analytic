{% extends 'dashboard.html.twig' %}

{% block title %}Visites!{% endblock %}

{% block content %}
{% include "top-bar.html.twig" with {'title':'Visits','btn':''} %}
<div class="card">
    <div class="card-body">
        <table id="example" class="card-body table table-striped dataTables_wrapper dt-bootstrap5" style="width:100%">
        <thead>
            <tr>
                <th>Title</th>
                <th>Start At</th>
                <th>End At</th>
                <th>Client</th>
                <th>ACTIONS</th>
            </tr>
        </thead>
        <tbody>
            {% for v in visites %}
                <tr>
                    <td>{{ v.title }}</td>
                    <td>{{ v.startTime|date('d/m/Y H:i')}}</td>
                    <td>{{ v.endTime|date('d/m/Y H:i')}}</td>
                    <td>{{ v.client.email }}</td>
                    <td><a href="{{ path('visit_edit', {'id': v.id })}}">
						<i class="fs-5 bi-pencil-square"></i>
					</a></td>
                </tr>
            {% endfor %}
        </tbody>
        
</table>
    </div>
</div>
{% endblock %}
{% block javascripts %}
{{ parent() }}
<script>

</script>
{% endblock %}